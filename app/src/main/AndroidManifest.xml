<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heima.transinapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestCallBack.TestCallBackActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".SharePreActivity"></activity> -->
        <!-- <activity android:name=".BroadActivity"></activity> -->
        <!--<activity android:name=".TestCallBack.TestCallBackActivity" />-->

        <service
            android:name=".TestCallBack.SendService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>